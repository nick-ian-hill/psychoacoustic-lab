{
  "name": "psychoacoustic-lab",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "example": "examples"
  },
  "scripts": {
    "dev": "npm run dev --prefix web-app",
    "build:web": "npm run build --prefix web-app",
    "build:mcp": "npm run build --prefix mcp-server",
    "build": "npm run build:web && npm run build:mcp",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3"
  }
}
